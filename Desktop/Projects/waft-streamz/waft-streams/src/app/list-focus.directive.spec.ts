import { ListFocusDirective } from './list-focus.directive';

describe('ListFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new ListFocusDirective();
    expect(directive).toBeTruthy();
  });
});
